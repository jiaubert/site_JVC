{% extends 'layout/layout.html.twig' %}

{% block body %}


    <a href="{{ path('admin_posts') }}" class="btn btn-outline-primary mt-2">Back to the list</a>

    <div class="container mt-5">


        <h2> {{ post.title }}</h2>

        <p>{{ post.content }}</p>
        <p>Number of views: {{ post.numberView }}</p>

    </div>

{% endblock %}
